import LoginForm from '@/components/LoginFrom/LoginForm';
import React from 'react';

const LoginPage = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default LoginPage;